# Core MCP framework (pinned to 2.13.x for compatibility)
fastmcp>=2.13.0,<2.14.0

# FastAPI for routing layer
# Latest stable version compatible with Starlette constraints
# fastapi>=0.115.0,<0.116.0

# ASGI server
uvicorn[standard]>=0.32.0,<1.0.0

# Starlette - compatible with both FastMCP and FastAPI
# FastAPI 0.115.x requires: starlette>=0.40.0,<0.51.0
# Using compatible range for both
starlette>=0.40.0,<0.51.0

# Data validation (required by FastAPI and FastMCP)
pydantic>=2.0.0,<3.0.0